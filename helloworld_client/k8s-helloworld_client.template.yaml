apiVersion: batch/v1beta1
kind: CronJob
metadata:
  creationTimestamp: null
  labels:
    run: $APPLICATION_CLIENT
  name: $APPLICATION_CLIENT
spec:
  concurrencyPolicy: Allow
  jobTemplate:
    metadata:
      creationTimestamp: null
    spec:
      template:
        metadata:
          creationTimestamp: null
          labels:
            run: $APPLICATION_CLIENT
        spec:
            containers:
            - image: gcr.io/$GOOGLE_CLOUD_PROJECT/$GOOGLE_CLOUD_PROJECT-labs/tracing_$APPLICATION_CLIENT
              name: $APPLICATION_CLIENT
              resources:
                requests:
                  cpu: 200m
            resources: {}
          restartPolicy: OnFailure
  schedule: '*/1 * * * *'
status: {}